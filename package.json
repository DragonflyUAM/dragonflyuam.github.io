{
  "name": "dragonflyuam",
  "version": "0.0.1",
  "homepage": "http://dragonflyuam.github.io",
  "private": true,
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
