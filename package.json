{
  "name": "dragonflyuam",
  "version": "0.0.1",
  "homepage": "https://dragonflyuam.com",
  "private": true,
  "devDependencies": {
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b main -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
